你是一位顶级前端工程师 + 视觉设计师，请生成一个完整可运行的 HTML 文件（包含 HTML + CSS + JavaScript），用于展示一个“PDF 演示动画”。

## 🎯 目标
创建一个电影级赛博朋克风格的交互式演示页面，用于展示 PDF 内容（可用模拟文本代替）。


## 🎨 视觉风格（必须严格执行）
- 风格：赛博朋克 / 科技未来感 / 高端 UI
- 渲染质感：8K 级别精细视觉（通过渐变、阴影、发光模拟）
- 主色调：深黑 + 电光蓝 + 荧光紫
- 整体氛围：类似 AI 数据中枢 / 黑客界面


## 🧱 场景构图
1. 页面中心：
   - 一个“半透明玻璃态（Glassmorphism）浮空面板”
   - 用于展示 PDF 内容（分页结构）
   - 带模糊背景 + 轻微反光 + 边框发光

2. 页面背景：
   - 深色背景（接近黑色）
   - 带“无限延伸的 3D 网格地面”
   - 网格带蓝紫渐变发光，并有轻微动态（流动/呼吸）


## ✨ 动态特效（必须实现）

### 1️⃣ 粒子系统（重点）
- 大量微小粒子（蓝色 + 紫色）
- 随机漂浮（类似数据流）
- 可使用 canvas 或 CSS/JS 实现
- 有轻微发光和透明度变化

### 2️⃣ 全息装饰元素
- 中央面板周围：
  - 2 个发光圆环
  - 缓慢旋转（不同方向）
- 背景：
  - 半透明六边形矩阵
  - 随机闪烁（opacity 动画）

### 3️⃣ 扫描光束（核心视觉）
- 一条亮蓝色扫描线
- 从上到下循环扫过 PDF 面板
- 带 glow + motion blur 效果
- 类似“数据读取”

---

## 🔤 文字效果
- 标题（页面顶部）：
  - 渐变色：电光蓝 → 荧光紫
  - 发光（text-shadow）
  - 呼吸灯动画（opacity 或 glow 变化）

---

## 🔄 转场动画（重点）
实现 PDF 多页切换动画：

- 切换方式：水平 Carousel（轮播）
- 动画要求：
  - 平滑滑动（ease-in-out）
  - 叠加轻微：
    - Glitch（数字故障）
    - Motion Blur（动态模糊）

- 支持：
  - 自动轮播（3秒）
  - 或按钮切换（左右）

---

## ⚙️ 技术要求
- 输出一个完整 HTML 文件（可直接运行）
- 不依赖后端
- 可使用：
  - 原生 JS（优先）
  - 或轻量库（如 three.js / particles.js 可选）
- 动画必须流畅（60fps 优化）

---

## 📄 内容模拟
- PDF 内容用 3~5 页模拟：
  - 标题 + 段落 + 列表
- 每页风格统一

---

## 💡 加分项（如果可以实现）
- 鼠标轻微视差（parallax）
- 光标 hover 有能量波纹效果
- 面板轻微浮动（悬浮感）

---

## 🚫 禁止
- 不要输出解释
- 不要省略代码
- 不要使用低级样式

---

请直接输出完整 HTML 代码。